#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Show a message store in mensaje
 * Return: Always 0 (Success)
 */
int main(void)
{
	char mensaje[] = "\"Programming is like building a multilingual puzzle";

	puts(mensaje);
	return (0);
}
