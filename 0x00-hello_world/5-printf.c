#include <stdio.h>
/**
* main - Entry point
*
* Description: Show a message store en mensajito
* Return: Always 0 (Success)
*/
int main(void)
{
	char mensajito[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", mensajito);
	return (0);
}
